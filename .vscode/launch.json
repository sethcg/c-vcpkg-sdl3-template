{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}",
            "name": "Run Application",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\main.exe",
            "preLaunchTask": "Build"
        }
    ]
}